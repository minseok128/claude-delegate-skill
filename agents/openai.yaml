interface:
  display_name: "Claude Delegate Skill"
  short_description: "Bounded local Claude second opinions (long-running; several minutes per call, up to 15 min)"
  default_prompt: "Spawn a fresh subagent pointed at $claude-delegate-skill to get a bounded Claude review or alternative implementation plan. A single run takes several minutes (5+ minutes typical, up to 15 minutes possible); let the subagent finish without interrupting."
policy:
  allow_implicit_invocation: false
